defaultTasks 'clean', 'build'

allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    apply from: "$rootDir/gradle/libraries.gradle"

    ext {
        version = '0.1-SNAPSHOT'
        group = 'io.github.fdayamani.hearts'
    }
}
